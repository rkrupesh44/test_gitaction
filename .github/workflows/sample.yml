name: Shell Command

on: push

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello IKEA"
      - name: multiple script
        run: |
          node -v
          npm -v